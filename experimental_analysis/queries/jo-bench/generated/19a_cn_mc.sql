SELECT * FROM movie_companies AS mc, company_name AS cn WHERE cn.country_code = '[us]' AND mc.note IS NOT NULL AND (mc.note LIKE '%(USA)%' OR mc.note LIKE '%(worldwide)%') AND cn.id = mc.company_id AND mc.company_id = cn.id;